import axios from "axios";
// import { LatLngTuple } from "react-leaflet";

// const calculateRoute = async (start: LatLngTuple, end: LatLngTuple, mode: string) => {
//     try {
//       const response = await axios.get(`https://api.openrouteservice.org/v2/directions/${mode}`, {
//         params: {
//           api_key: process.env.NEXT_PUBLIC_OPENROUTESERVICE_API_KEY,
//           start: `${start[1]},${start[0]}`,
//           end: `${end[1]},${end[0]}`,
//         },
//       });
//       return response.data;
//     } catch (error) {
//       console.error('Error calculating route:', error);
//       return null;
//     }
//   };
  